[package]
name = "fpga_dat2tcp"
version = "0.1.0"
edition = "2024"

[dependencies]
memmap2 = "0.9.5"
libc = "0.2"
nix = { version = "0.31.2", features = ["poll"] }
# serde = { version = "1.0.118", features = ["derive"] }
